@model Acc.Api.Models.Dto.View.AccountUserDto

@{
    Layout = "../Shared/_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <title>title</title>
    <nav class="bg-white border-gray-200 px-2 sm:px-4 py-2.5 rounded dark:bg-gray-900">
      <div class="container flex flex-wrap items-center justify-end mx-auto">
       
          <button type="button" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none 
                focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-3 md:mr-0 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
                  onclick="window.location.href='@Url.Action("Logout","Auth")'">Logout</button>
 </div>
    </nav>
</head>
<body>
<p>This is index razor page.</p>

  <table class="table-fixed">
            <thead>
            <tr>
                <th>Account Name</th>
                <th>Role</th>
                <th>UserEmail</th>
                
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>@Model.AccountName </td>
                <td>@Model.UserRoleId</td>
                 <td>@Model.UserFullName</td>
            </tr>
            </tbody>
        </table>
<div>

</div>
</body>
</html>




